import { createAction } from 'redux-actions'

export const addLocation = createAction('add location')
export const deleteLocation = createAction('delete location')
export const editLocation = createAction('edit location')
