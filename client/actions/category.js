import { createAction } from 'redux-actions'

export const addCategory = createAction('add category')
export const deleteCategory = createAction('delete category')
export const editCategory = createAction('edit category')
